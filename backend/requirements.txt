# Web framework
fastapi>=0.110.0
uvicorn[standard]>=0.27.0

# Authentication
python-jose[cryptography]>=3.3.0
passlib[bcrypt]>=1.7.4
bcrypt==4.0.1

# Settings & validation
pydantic-settings>=2.0.0
email-validator>=2.0.0

# Database (async MongoDB)
motor>=3.3.0

# HTTP client
httpx>=0.27.0

# AI / LLM
langchain>=0.2.0
langchain-core>=0.2.0
langchain-groq>=0.1.0
langchain-community>=0.2.0
langchain-text-splitters>=0.2.0
langsmith>=0.1.0

# Embeddings via HuggingFace hosted Inference API — served over HTTP through
# langchain-community (above) + huggingface_hub, so no local torch/transformers.
huggingface_hub>=0.23.0

# Vector store
chromadb>=0.5.0
# chromadb 0.5.x calls posthog.capture() positionally; posthog 3+ broke that
# signature and spams "Failed to send telemetry event" errors. Pin to 2.x.
posthog>=2.4.0,<3.0

# PDF processing
pdfplumber>=0.10.0
PyPDF2>=3.0.0

# DOCX processing
python-docx>=1.1.0
python-multipart